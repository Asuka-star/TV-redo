spring:
  application:
    name: post-service # 服务名称
  cloud:
    nacos:
      server-addr: 192.168.145.128:8848 # nacos地址
      discovery:
        namespace: 123
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - data-id: shared-jdbc.yaml # 共享mybatis配置
          - data-id: shared-swagger.yaml # 共享文档配置
          - data-id: shared-dubbo.yaml # 共享dubbo配置
        namespace: 123